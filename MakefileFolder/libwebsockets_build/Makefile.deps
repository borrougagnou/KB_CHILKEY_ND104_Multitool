# Makefile.deps

.DEFAULT_GOAL := all

## Global variables

# $(CURDIR) = MakefileFolder/libwebsockets_build
ROOT_DIR    := $(CURDIR)/../..
BUILD_BASE  := $(ROOT_DIR)/build/lws_deps
SRC_DIR     := $(BUILD_BASE)/src
BUILD_DIR   := $(BUILD_BASE)/obj


## Platform detection
## (Run uname, but should fallback to Windows)
UNAME_S := $(shell uname -s 2>/dev/null || echo Windows)
UNAME_M := $(shell uname -m 2>/dev/null || echo $(PROCESSOR_ARCHITECTURE))

### OS Detection
ifeq ($(UNAME_S),Linux)
    OS_NAME := Linux
else ifeq ($(UNAME_S),Darwin)
    OS_NAME := Mac
else ifneq (,$(findstring MINGW,$(UNAME_S)))
    OS_NAME := Windows
else ifneq (,$(findstring MSYS,$(UNAME_S)))
    OS_NAME := Windows
else ifneq (,$(findstring UCRT,$(UNAME_S)))
    OS_NAME := Windows
else
    # fallback
    ifeq ($(OS),Windows_NT)
        OS_NAME := Windows
    else
        $(error Cannot detect platform. Use Windows MSYS2 MINGW32/MINGW64, Linux, or macOS.)
    endif
endif

### Arch detection
ifeq ($(OS_NAME),Windows)
    ifeq ($(UNAME_M),x86_64)
        ARCH ?= x64
    else ifeq ($(UNAME_M),i686)
        ARCH ?= x32
    else ifeq ($(MSYSTEM),MINGW64)
        ARCH ?= x64
    else ifeq ($(MSYSTEM),MINGW32)
        ARCH ?= x32
    else ifeq ($(MSYSTEM),UCRT64)
        ARCH ?= x64
    # fallback
    else ifeq ($(PROCESSOR_ARCHITECTURE),AMD64)
        ARCH ?= x64
    else ifeq ($(PROCESSOR_ARCHITECTURE),x86)
        ARCH ?= x32
    else
        ARCH ?= x64
    endif
    OS_FOLDER := Windows-$(ARCH)
else
    OS_FOLDER := $(OS_NAME)
endif

# Final clean install destination
INSTALL_DIR := $(ROOT_DIR)/src/external/libwebsockets/$(OS_FOLDER)

# CMake Flags
CMAKE_FLAGS := -DCMAKE_INSTALL_PREFIX=$(INSTALL_DIR) \
               -DBUILD_SHARED_LIBS=OFF \
               -DCMAKE_BUILD_TYPE=Release

# Windows XP SP3 Support Flag
ifeq ($(OS_NAME),Windows)
    CMAKE_FLAGS += -DCMAKE_C_FLAGS="-D_WIN32_WINNT=0x0501" \
                   -DCMAKE_CXX_FLAGS="-D_WIN32_WINNT=0x0501"
endif


## BUILD PART


all: zlib mbedtls nghttp2 libwebsockets

zlib:
	@mkdir -p $(SRC_DIR) $(BUILD_DIR)
	@if [ ! -d "$(SRC_DIR)/zlib-src" ]; then \
		git clone --depth 1 --branch v1.3.1 https://github.com/madler/zlib.git $(SRC_DIR)/zlib-src; \
	fi
	cmake -S $(SRC_DIR)/zlib-src -B $(BUILD_DIR)/zlib-build $(CMAKE_FLAGS)
	cmake --build $(BUILD_DIR)/zlib-build --target install

mbedtls:
	@if [ ! -d "$(SRC_DIR)/mbedtls-src" ]; then \
		git clone --depth 1 --branch v2.28.8 https://github.com/Mbed-TLS/mbedtls.git $(SRC_DIR)/mbedtls-src; \
	fi
	cmake -S $(SRC_DIR)/mbedtls-src -B $(BUILD_DIR)/mbedtls-build $(CMAKE_FLAGS) -DENABLE_TESTING=OFF -DENABLE_PROGRAMS=OFF
	cmake --build $(BUILD_DIR)/mbedtls-build --target install

nghttp2:
	@if [ ! -d "$(SRC_DIR)/nghttp2-src" ]; then \
		git clone --depth 1 --branch v1.62.1 https://github.com/nghttp2/nghttp2.git $(SRC_DIR)/nghttp2-src; \
	fi
	cmake -S $(SRC_DIR)/nghttp2-src -B $(BUILD_DIR)/nghttp2-build $(CMAKE_FLAGS) -DENABLE_LIB_ONLY=ON -DENABLE_STATIC_LIB=ON
	cmake --build $(BUILD_DIR)/nghttp2-build --target install

libwebsockets: zlib mbedtls nghttp2
	@if [ ! -d "$(SRC_DIR)/lws-src" ]; then \
		git clone --depth 1 --branch v4.3.3 https://github.com/warmcat/libwebsockets.git $(SRC_DIR)/lws-src; \
	fi
	cmake -S $(SRC_DIR)/lws-src -B $(BUILD_DIR)/lws-build $(CMAKE_FLAGS) \
		-DLWS_WITH_SSL=ON -DLWS_WITH_MBEDTLS=ON \
		-DLWS_WITH_ZLIB=ON -DLWS_WITH_HTTP2=ON \
		-DLWS_WITH_STATIC=ON -DLWS_WITH_SHARED=OFF \
		-DCMAKE_PREFIX_PATH=$(INSTALL_DIR)
	cmake --build $(BUILD_DIR)/lws-build --target install


## CLEAN PART

clean-build:
	@echo "Cleaning temporary build and source directories..."
	rm -rf $(BUILD_BASE)

clean-install:
	@echo "Cleaning installed external libraries..."
	rm -rf $(INSTALL_DIR)

clean: clean-build clean-install
	@echo "Full clean complete."

.PHONY: all zlib mbedtls nghttp2 libwebsockets clean clean-build clean-install
